server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # Client-side routes such as /boards/:id must fall back to index.html,
  # otherwise a page reload on a board URL returns 404 from nginx.
  location / {
    try_files $uri $uri/ /index.html;
  }
}
